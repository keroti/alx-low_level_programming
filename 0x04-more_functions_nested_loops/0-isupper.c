#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks for uppercase
 *
 * Return: 1 if 'c' is uppercase
 * and Return: 0 if 'c' is lowercase
 */

int _isupper(int c)
{
	int c;

	if (c >= 'A' && c <= 'B')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}	
